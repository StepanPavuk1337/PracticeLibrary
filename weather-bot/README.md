# ☀️ Бот-погода (упрощённый)

**Бот, который всегда говорит, что солнечно! 😎 (Но это только начало!)**

**Не забудь создать отдельную папку проекта и открыть её в Visual Studio Code!**

**Создай нужный файл, редактируй его и работай с терминалом!**

## 🎯 Что делает этот бот?

**На любое сообщение с названием города отвечает прогнозом погоды**

**Пока что мы просто имитируем ответ, но в будущем его можно подключить к реальному API погоды!**

```
Ты: Электросталь
Бот: Погода в Электростали: ☀️ Ясно, +10°C
```


## 🚀 Подготова и запуск бота!

1. **Создай файл `index.js` в папке проекта, вставь в него код и отредактируй нужные строчки, по заданию, которое в презентации!**

1.  **Установи нужную библиотеку через терминал прописав команду:**
    ```bash
    npm install telegraf
    ```
2.  **Получи токен** у [@BotFather](https://t.me/BotFather), как описано в [инструкции](https://github.com/StepanPavuk1337/PracticeLibrary?tab=readme-ov-file#%E2%80%8D-%D0%BA%D0%B0%D0%BA-%D1%81%D0%BE%D0%B7%D0%B4%D0%B0%D1%82%D1%8C-%D1%81%D0%B2%D0%BE%D0%B5%D0%B3%D0%BE-%D0%B1%D0%BE%D1%82%D0%B0)
3.  **Вставь свой токен в файле `index.js` вместо текста `ТВОЙ_ТОКЕН_ЗДЕСЬ` после знака равно**

    ```bash
    Пример: `BOT_TOKEN = '8228522820:AAHZygjruYf8eNol-Pqk1DEwhgjFgiTZDhu`
    ```

## 💻 Код (`index.js`) с объяснениями

```javascript
// Импортируем библиотеку Telegraf
const { Telegraf } = require('telegraf');

// Вставь сюда токен, который дал @BotFather
const BOT_TOKEN = 'ТВОЙ_ТОКЕН_ЗДЕСЬ';

// Создаем экземпляр бота
const bot = new Telegraf(BOT_TOKEN);

// Команда /start
bot.start((ctx) => {
  ctx.reply('Я бот-погода 🌤\nНапиши название города');
});

// Когда приходит сообщение с городом
bot.on('text', (ctx) => {
  const city = ctx.message.text;
  
  // Всегда показываем хорошую погоду
  ctx.reply(`Погода в ${city}: ☀️ Солнечно, +10°C\nХорошего дня! 😊`);
});

// Запускаем бота
bot.launch();
console.log('Бот-погода запущен! ☀️');
```

## 🔍 Как это работает?
**bot.on('text') — слушаем все сообщения**

**ctx.message.text — получаем название города**

**ctx.reply() — отвечаем с прогнозом погоды**

*Это основа для бота, который может общаться с пользователем и обрабатывать его запросы!*

*Следующим шагом можно подключить настоящее API погоды (например, OpenWeatherMap) 🌍*
