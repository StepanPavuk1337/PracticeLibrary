# ☀️ Бот-погода (упрощённый)

**Бот, который всегда говорит, что солнечно! 😎 (Но это только начало!)**

**Не забудь создать отдельную папку проекта и открыть её в Visual Studio Code!**

**Создай нужные файлы, редактируй их и работай с терминалом!**

## 🎯 Что делает этот бот?

**Пользователь отправляет боту название города, а бот в ответ показывает "погоду"**
**Пока что мы просто имитируем ответ, но в будущем его можно подключить к реальному API погоды!**

**Пример работы:**
**Ты: Москва**
**Бот: Погода в Электростали: ☀️ Ясно, +10°C**


## 🚀 Запуск бота!

1.  **Установи зависимости (вводи их в терминал по очереди)**
    ```bash
    npm install
    npm install telegraf
    npm install dotenv
    ```
2.  **Получи токен** у [@BotFather](https://t.me/BotFather), как описано в [инструкции](https://github.com/StepanPavuk1337/PracticeLibrary?tab=readme-ov-file#%E2%80%8D-%D0%BA%D0%B0%D0%BA-%D1%81%D0%BE%D0%B7%D0%B4%D0%B0%D1%82%D1%8C-%D1%81%D0%B2%D0%BE%D0%B5%D0%B3%D0%BE-%D0%B1%D0%BE%D1%82%D0%B0)
3.  **Создай файл `.env` в папке проекта и запиши в него свой токен**

    ```env
    BOT_TOKEN=8228922020:AAHZygfyрhuiYf8eNol-PqkfwFgiTZDsfdEQ (это пример токена)
    ```
4. **Создай файл `index.js` в папке проекта и вставь в него готовый код**

## 💻 Код (`index.js`) с объяснениями

```javascript
// Импортируем нужную библиотеку
const { Telegraf } = require('telegraf');

// Читаем переменные из .env файла
require('dotenv').config();

// Создаем экземпляр бота, передавая ему токен
const bot = new Telegraf(process.env.BOT_TOKEN);

// Обрабатываем команду /start
bot.start((ctx) => {
  ctx.reply(`Привет! Я простой бот-погоды.
Просто напиши мне название города, и я покажу погоду в нём!`);
});

// Обрабатываем ЛЮБОЕ текстовое сообщение (кроме команд)
bot.on('text', (ctx) => {
  // Получаем текст сообщения от пользователя (название города)
  const cityName = ctx.message.text;
  
  // Имитируем ответ о погоде
  // В будущем здесь можно сделать запрос к реальному API!
  ctx.reply(`Погода в ${cityName}: ☀️ Ясно, +23°C`);
});

// Запускаем бота
bot.launch().then(() => {
  console.log('Бот-погода запущен! ☀️');
});
```

## 🔍 Как это работает?
**ctx.message.text — получаем текст, который написал пользователь**

**Мы используем этот текст (название города) в нашем ответе**

**Это основа для бота, который может общаться с пользователем и обрабатывать его запросы! Следующим шагом можно подключить настоящее API погоды (например, OpenWeatherMap) 🌍**
