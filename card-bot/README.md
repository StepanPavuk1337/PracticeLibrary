# 🪪 Бот-визитка

**Самый первый и простой бот, который познакомит тебя с основами!**

**Не забудь создать отдельную папку проекта и открыть её в Visual Studio Code!**

**Создай нужный файл, редактируй его и работай с терминалом!**

## 🎯 Что делает этот бот?

**Отвечает заранее заданным приветственным текстом на команду `/start` или на любое другое сообщение**

## 🚀 Подготова и запуск бота!

1. **Создай файл `index.js` в папке проекта, вставь в него код и отредактируй нужные строчки, которые скажет инструктор!**

1.  **Установи нужную библиотеку через терминал прописав команду:**
    ```bash
    npm install telegraf
    ```
2.  **Получи токен** у [@BotFather](https://t.me/BotFather), как описано в [инструкции](https://github.com/StepanPavuk1337/PracticeLibrary?tab=readme-ov-file#%E2%80%8D-%D0%BA%D0%B0%D0%BA-%D1%81%D0%BE%D0%B7%D0%B4%D0%B0%D1%82%D1%8C-%D1%81%D0%B2%D0%BE%D0%B5%D0%B3%D0%BE-%D0%B1%D0%BE%D1%82%D0%B0)
3.  **Вставь свой токен в файле `index.js` вместо текста `ТВОЙ_ТОКЕН_ЗДЕСЬ` после знака равно**

    ```bash
    Пример: `BOT_TOKEN = '8228522820:AAHZygjruYf8eNol-Pqk1DEwhgjFgiTZDhu';
    ```

## 💻 Код (`index.js`) с объяснениями


```javascript
// Импортируем библиотеку Telegraf
const { Telegraf } = require('telegraf');

// Вставь сюда токен, который дал @BotFather
const BOT_TOKEN = 'ТВОЙ_ТОКЕН_ЗДЕСЬ';

// Создаем экземпляр бота
const bot = new Telegraf(BOT_TOKEN);

// Обрабатываем команду /start
bot.start((ctx) => {
  // ctx - контекст сообщения, ctx.reply - ответить пользователю
  ctx.reply(
  `Привет! Я бот, который познакомит с нашим проектом: <имя проекта> 📇
    - Наша команда: (ссылка на картинку про команду)
    - Наш проект: (ссылка на картинки про проект)
    - Наш цифровой двойник: (ссылка на видео про #Мойцифровойдвойник)

    Для связи: contact@example.com`);
});

// Обрабатываем ЛЮБОЕ текстовое сообщение
bot.on('text', (ctx) => {
  ctx.reply('Для начала работы отправь команду /start ✨');
});

// Обрабатываем команду /help
bot.help((ctx) => {
  ctx.reply('Я бот-визитка! Просто напиши /start чтобы начать. 🤖');
});

// Запускаем бота
bot.launch()
  .then(() => {
    console.log('Бот-визитка запущен! 🚀');
  })
  .catch((err) => {
    console.error('❌ Ошибка запуска бота:', err.message);
  });
```

 **После того как ты вставил код, чтобы запустить бота пропиши в терминал:**
 
    node index.js

**Увидишь в терминале сообщение: `Бот-визитка запущен! 🚀` - поздравляю, твой бот готов к работе! 🎉**

## 🔍 Как это работает?

**bot.start() — "слушает" команду /start**

**bot.on('text') — "слушает" любое текстовое сообщение**

**ctx.reply() — бот отвечает пользователю**

*Всё просто! Ты создал своего первого Telegram-бота! 🥳*
