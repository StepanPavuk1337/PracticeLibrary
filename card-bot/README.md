# 🪪 Бот-визитка

Самый первый и простой бот, который познакомит тебя с основами!

## 🎯 Что делает этот бот?

Отвечает заранее заданным приветственным текстом на команду `/start` или на любое другое сообщение

## 📁 Файлы в проекте

- `index.js` — главный файл с кодом нашего бота
- `.env` — файл для хранения токена бота

## 🚀 Быстрый старт

1.  **Получи токен** у [@BotFather](https://t.me/BotFather), как описано в инструкции
2.  **Создай файл** `.env` в папке проекта и запиши в него свой токен

    ```env
    BOT_TOKEN=8228922020:AAHZygfyрhuiYf8eNol-PqkfwFgiTZDsfdEQ (это пример токена)
    ```
4.  **Пропиши в терминал, чтобы запустить бота:**
5.  
    ```bash
    node index.js
    ```

**Увидишь сообщение: `Бот-визитка запущен! 🚀` - поздравляю, твой бот готов к работе! 🎉**

## 💻 Код (`index.js`) с объяснениями

```javascript
// Импортируем нужную библиотеку
const { Telegraf } = require('telegraf');
// Читаем переменные из .env файла
require('dotenv').config();

// Создаем экземпляр бота, передавая ему токен
const bot = new Telegraf(process.env.BOT_TOKEN);

// Обрабатываем команду /start
// ctx - это контекст, в нем есть вся информация о сообщении
bot.start((ctx) => {
// ctx.reply - ответить тому, кто написал
  ctx.reply(`Привет! Я бот-визитка.
Здесь вы можете узнать о нашем проекте.
Наш сайт: example.com`);
});

// Обрабатываем любое текстовое сообщение
bot.on('text', (ctx) => {
  ctx.reply('Для начала работы отправь команду /start');
});

// Запускаем нашего бота
bot.launch().then(() => {
  console.log('Бот-визитка запущен! 🚀');
});
