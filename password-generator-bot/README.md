# 🔐 Бот-генератор паролей

**Создавай надёжные пароли любой длины прямо в Telegram! 💪**

**Не забудь создать отдельную папку проекта и открыть её в Visual Studio Code!**

**Создай нужные файлы, редактируй их и работай с терминалом!**

## 🎯 Что делает этот бот?

**По команде `/password <длина>` бот генерирует случайный, сложный пароль заданной длины**

## 📁 Основные файлы в папке проекта

- `index.js` — главный файл с кодом нашего бота
- `.env` — файл для хранения токена бота


## 🚀 Запуск бота!

1.  **Установи зависимости (вводи их в терминал по очереди)**
    ```bash
    npm install
    npm install telegraf
    npm install dotenv
    ```
2.  **Получи токен** у [@BotFather](https://t.me/BotFather), как описано в [инструкции](https://github.com/StepanPavuk1337/PracticeLibrary?tab=readme-ov-file#%E2%80%8D-%D0%BA%D0%B0%D0%BA-%D1%81%D0%BE%D0%B7%D0%B4%D0%B0%D1%82%D1%8C-%D1%81%D0%B2%D0%BE%D0%B5%D0%B3%D0%BE-%D0%B1%D0%BE%D1%82%D0%B0)
3.  **Создай файл `.env` в папке проекта и запиши в него свой токен**

    ```env
    BOT_TOKEN=8228922020:AAHZygfyрhuiYf8eNol-PqkfwFgiTZDsfdEQ (это пример токена)
    ```
4. **Создай файл `index.js` в папке проекта и вставь в него готовый код**

## 💻 Код (`index.js`) с объяснениями

```javascript

// Импортируем нужную библиотеку
const { Telegraf } = require('telegraf');

// Читаем переменные из .env файла
require('dotenv').config();

// Создаем экземпляр бота, передавая ему токен
const bot = new Telegraf(process.env.BOT_TOKEN);

// Обрабатываем команду /password
bot.command('password', (ctx) => {
  // ctx.message.text содержит полный текст сообщения, например: "/password 12"
  const commandText = ctx.message.text;
  
  // Разбиваем текст на части: ["/password", "12"]
  const parts = commandText.split(' ');
  
  // Проверяем, указал ли пользователь длину
  if (parts.length < 2) {
    return ctx.reply('Пожалуйста, укажи длину пароля. Например: /password 12');
  }
  
  // Парсим длину пароля (второй элемент массива)
  const length = parseInt(parts[1]);
  
  // Проверяем, что длина - это число
  if (isNaN(length)) {
    return ctx.reply('Длина должна быть числом! Например: /password 12');
  }

  // Проверяем, что длина не слишком маленькая
  if (length < 4) {
    return ctx.reply('Пароль должен быть длиннее 3 символов!');
  }

  // Создаём символы, из которых будет состоять пароль
  const charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*';
  let password = '';
  
  // В цикле генерируем каждый символ пароля
  for (let i = 0; i < length; i++) {
    // Выбираем случайный символ из строки charset
    const randomIndex = Math.floor(Math.random() * charset.length);
    password += charset[randomIndex];
  }
  
  // Отправляем пароль пользователю
  ctx.reply(`🔐 Вот твой надёжный пароль:\n<code>${password}</code>`, { parse_mode: 'HTML' });
});

// Запускаем бота
bot.launch().then(() => {
  console.log('Бот-генератор паролей запущен! 🔐');
});
```

 **После того как ты вставил код, чтобы запустить бота пропиши в терминал:**
 
    node index.js

**Увидишь в терминале сообщение: `Бот-генератор паролей запущен! 🔐` - поздравляю, твой бот готов к работе! 🎉**

## 🔍 Как это работает?
**commandText.split(' ') — разбиваем сообщение на части, чтобы получить длину пароля**

**parseInt() — превращаем строку "12" в число 12**

**Цикл for — повторяем операцию нужное количество раз**

**charset[randomIndex] — берём случайный символ из нашей строки-алфавита**

**{ parse_mode: 'HTML' } — позволяет отображать пароль моноширинным шрифтом**

*Теперь ты умеешь работать с аргументами команд и генерировать сложные строки! 🎯*
