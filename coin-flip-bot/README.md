# 🪙 Бот "Орёл или Решка"

**Бот, который решает твои споры честным подбрасыванием монетки! 🎲**

**Не забудь создать папку на рабочем столе и открыть её в VS code!**

**Создай нужные файлы, редактируй их и работай с терминалом!**

## 🎯 Что делает этот бот?

По команде `/flip` бот "подбрасывает" монетку и случайным образом выдает результат: "Орёл" или "Решка".

## 📁 Файлы в проекте

- `index.js` — главный файл с кодом нашего бота
- `.env` — файл для хранения токена бота

## 🚀 Быстрый старт

1.  **Получи токен** у [@BotFather](https://t.me/BotFather), как описано в [инструкции](https://github.com/StepanPavuk1337/PracticeLibrary?tab=readme-ov-file#%E2%80%8D-%D0%BA%D0%B0%D0%BA-%D1%81%D0%BE%D0%B7%D0%B4%D0%B0%D1%82%D1%8C-%D1%81%D0%B2%D0%BE%D0%B5%D0%B3%D0%BE-%D0%B1%D0%BE%D1%82%D0%B0)
2.  **Создай файл** `.env` в папке проекта и запиши в него свой токен

    ```env
    BOT_TOKEN=8228922020:AAHZygfyрhuiYf8eNol-PqkfwFgiTZDsfdEQ (это пример токена)
    ```
3.  **После вставки кода пропиши в терминал, чтобы запустить бота:**
    
    ```bash
    node index.js
    ```

**Увидишь сообщение: `Бот-визитка запущен! 🚀` - поздравляю, твой бот готов к работе! 🎉**

## 💻 Код (`index.js`) с объяснениями

```javascript
const { Telegraf } = require('telegraf');
require('dotenv').config();

const bot = new Telegraf(process.env.BOT_TOKEN);

// Обрабатываем команду /flip
bot.command('flip', (ctx) => {
  // Генерируем случайное число: 0 или 1
  const randomNumber = Math.floor(Math.random() * 2);
  
  // Если randomNumber равен 0 - будет "Орёл", иначе - "Решка"
  const side = randomNumber === 0 ? 'Орёл' : 'Решка';
  
  // Отправляем результат с эмодзи монетки
  ctx.reply(`🪙 Монетка подброшена! Выпало: ${side}!`);
});

// Запускаем бота
bot.launch().then(() => {
  console.log('Бот "Орёл или Решка" запущен! 🪙');
});
🔍 Как это работает?
Math.random() — генерирует случайное число от 0 до 1.

Math.floor() — округляет число вниз.

Math.floor(Math.random() * 2) — получаем случайно 0 или 1.

условие ? значение_если_да : значение_если_нет — это тернарный оператор, короткая замена if/else.

Ты научился работать со случайностью и простыми условиями! 👍
